name: Go

on:
  push:
    branches:
    - 'release-1.0'
  pull_request:
    branches:
    - 'release-1.0'

jobs:
  verify:
    name: Test
    runs-on: ubuntu-20.04
    steps:
    - name: test
      run: echo release-1.0

